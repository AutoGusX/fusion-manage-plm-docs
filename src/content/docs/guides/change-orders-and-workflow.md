---
title: Change Orders and Workflow
description: A task-oriented change-order lifecycle walkthrough, from creation through affected-items and transitions.
---

:::note[Coming soon]
This page is a placeholder. Content will walk through a CO lifecycle end to end, cross-linking to `api/v3/workflow` and `api/v3/relationships-and-affected-items`.

**Primary sources to author from:** `plm.js` workflow and relationship functions (BOM Builder Fork extension) and `client.py`.
:::

:::caution[Needs live verification]
This guide inherits the `views/2` vs `views/11` direction ambiguity documented in `api/v3/relationships-and-affected-items` — point to that page rather than restating or silently picking one direction as correct.
:::
