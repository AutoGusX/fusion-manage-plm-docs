---
title: Workspaces
description: List and retrieve workspaces, workspace sections/fields, and lookups by systemName.
---

:::note[Coming soon]
This page is a placeholder. Content will cover: `GET /api/rest/v1/workspaces` (list), `GET /api/v3/workspaces?offset=&limit=` (v3 paginated list), `GET /api/v3/workspaces/{wsId}/sections`, and lookup-by-`systemName` (e.g. `WS_CHANGE_APPROVAL_TEMPLATES`).

**Primary sources to author from:** the Onboarding Extension's `docs/API-INTEGRATION.md`, and `api-reference.md` in the Fusion Manage MCP server project.
:::
