---
title: Views, Fields, and Tableaus
description: Discover workspace fields, sections, and views, plus tableau CRUD — the prerequisite reference for field IDs used elsewhere.
---

:::note[Coming soon]
This page is a placeholder. Content will cover `fetchTabs`, `fetchSections`, `fetchFields`, `getWorkspaceViewFields`, BOM view discovery (`getBomViews`, `getBomViewsAndFields`, `getBomViewByName`), and tableau CRUD (`getTableaus`, `initTableau`, `addTableau`, `cloneTableau`).

**Primary sources to author from:** `plm.js` (BOM Builder Fork extension) and `SPECIFICATION-Fusion-Manage-Word-Document-Management.md` §5.1 (Workspace Views/Tableaus). Note: the view-1 fields endpoint lacks `editability`/`derived`/`derivedFieldSource`/`fieldValidators` metadata that `/api/v3/workspaces/{ws}/fields` exposes — call this out once authored (per `CLONE_FIELD_RULES.md`).
:::
