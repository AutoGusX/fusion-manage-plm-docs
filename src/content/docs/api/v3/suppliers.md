---
title: Suppliers
description: Retrieve approved suppliers and quotes for an item via the AML (views/8) pattern.
---

:::note[Coming soon]
This page is a placeholder. Content will cover `GET /api/v3/workspaces/{ws}/items/{id}/views/8/suppliers` and `.../suppliers/{supplierId}/quotes`, including the effective-lead-time calculation (`leadTime * leadTimeMultiplier`).

**Primary sources to author from:** `plm.js` (BOM Builder Fork extension) and `client.py`'s `get_item_suppliers`/`get_item_supplier_quotes`.
:::
