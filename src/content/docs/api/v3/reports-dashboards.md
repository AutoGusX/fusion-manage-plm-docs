---
title: Reports and Dashboards
description: Retrieve saved reports, dashboard charts, bookmarks, and recently-viewed items.
---

:::note[Coming soon]
This page is a placeholder. Content will cover `GET /api/rest/v1/reports/{reportId}`, plus bookmarks/recent-items/dashboard endpoints.

**Primary sources to author from:** `api-reference.md` (Fusion Manage MCP server) for the Get Report shape, and a full grep of `plm.js` (BOM Builder Fork extension) for `getRecentItems`, `getBookmarks`, `addBookmark`, `removeBookmark`, and dashboard-chart functions beyond what's been scanned so far — this page needs a fuller pass over `plm.js` before authoring.
:::
