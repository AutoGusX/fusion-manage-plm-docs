---
title: Property Instances
description: Retrieve classification-driven property instances and lookup values used to populate picklists.
---

:::note[Coming soon]
This page is a placeholder. Content will cover `GET /api/v2/property-instances?classification={id}&inherited=true` and its `/properties` sub-resource for display name/type.

**Primary source to author from:** `plm.js`'s `getPropertyInstances`/`getLookup` (BOM Builder Fork extension).
:::
