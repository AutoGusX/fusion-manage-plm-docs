---
title: Classifications
description: Retrieve the classification tree and its adjacency graph — the v2 classification subsystem, distinct from v3 items.
---

:::note[Coming soon]
This page is a placeholder. Content will cover `GET /api/v2/classifications` (paginated, `excludeAttribute=effectiveSchema`) and `GET /api/v2/classifications/{rootId}/graphs/adjacency-set`.

**Primary source to author from:** `plm.js`'s `getClassifications` (BOM Builder Fork extension). This subsystem currently has no known v3 equivalent — see `concepts/versioning` for why v2 isn't "the old v3."
:::
