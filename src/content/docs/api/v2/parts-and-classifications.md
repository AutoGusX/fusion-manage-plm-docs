---
title: Parts and Classifications
description: Link a workspace part to the classification tree and look up a part's assigned classifications.
---

:::note[Coming soon]
This page is a placeholder. Content will cover `GET /api/v2/parts?referenceUrn=urn:adsk.plm:tenant.workspace:{TENANT}/{workspaceId}` and `GET /api/v2/parts/{partId}/classifications`.

**Primary source to author from:** `plm.js`'s `getWorkspaceParts`/`getPartClassifications` (BOM Builder Fork extension).
:::
